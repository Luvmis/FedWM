# FedWM: Federated World Models

This repository contains the implementation of federated learning algorithms **FedWM**.

## 🛠 Environment Setup

```bash
pip install -r requirements.txt
```

and see `env/setup_scripts` to setup atari. 


---

## Usage

```bash
 python3 fedwm.py --configs atari100k --task atari_assault --logdir ./logdir/assault 
```

`task` is the game type, and `logdir` is location of log.

---



